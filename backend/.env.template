APP_CONFIG__DB__PG__URL=postgresql+asyncpg://user:password@localhost:5432/db
APP_CONFIG__DB__PG__ECHO=0
APP_CONFIG__AUTH__JWT__RESET_PASSWORD_TOKEN_SECRET=token
APP_CONFIG__AUTH__JWT__VERIFICATION_TOKEN_SECRET=token
APP_CONFIG__DB__REDIS__URL=redis://localhost:4536/5

# CORS (defaults allow all — override in production)
APP_CONFIG__CORS__ALLOWED_ORIGINS=["https://yourdomain.com"]
APP_CONFIG__CORS__ALLOW_CREDENTIALS=true